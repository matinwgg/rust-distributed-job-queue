[package]
name = "distributed_job_queue"
version = "0.1.0"
edition = "2021"
description = "A small, correctness-first distributed job queue prototype in Rust."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
